@using SIMVer.Services
@using SIMVer.Common
<div class="row-fluid">
<div class="span3 control-group">
	<label class="control-label">MSISDN/Id</label>
	<div class="controls">
		<input type="text" name="query" style="width: 100%" class="m-wrap" value="@Request["query"]"/>
	</div>
</div>
<div class="span3 control-group">
	<label class="control-label">@I18N.GetText("ProcessInstance.Status")</label>
	<div class="controls">
		<select class="m-wrap" data-placeholder="Wybierz status procesu" tabindex="1" name="Status">
			<option value=""></option>
            @foreach(var v in Enum.GetValues(typeof(SIMVer.Common.ProcessStatus))) {
                var sel = string.Equals(Request["Status"], ((int)v).ToString());
                <option value="@((int) v)" @(sel ? "selected" : "")>@I18N.GetText(v)</option>
            }
		</select>
	</div>
</div>
    <div class="span3 control-group">
	<label class="control-label">@I18N.GetText("ProcessInstance.ExitCode")</label>
	<div class="controls">
		<input type="text" name="ExitCode" style="width: 100%" class="m-wrap" value="@Request["ExitCode"]"/>
	</div>
</div>
</div>